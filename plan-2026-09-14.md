# 改進計畫 — 2026-09-14

承接 `code-review-2026-09-11.md` 與 `spec-signal-research-rework.md`。這份計畫的
根據是 2026-09-13 修復基礎設施之後，第一批可信資料上跑出來的檢定結果。

---

## 1. 目前確定知道的事

### 1.1 基礎設施已經健康（2026-09-13 修復）

| | 修復前 | 現在 |
|---|---|---|
| BTC 寫入間隔 p50 | 47.15 秒 | 1.7 秒 |
| 記憶體 | 1.71 GiB 持續爬升至 SIGSEGV | ~1.25 GiB 穩態 |
| 25 小時內重啟次數 | — | 0 |
| 24.6 小時資料量 | — | 2,427,880 筆 × 46 symbols |

根因是 `LocalOrderBook.to_model()` 每次 publish 都完整排序整本 `Decimal` 帳，
而 `@depth@100ms` 是差分串流、遠端價位幾乎不會被歸零，所以帳本會隨行程壽命
無限成長。這一項佔了 73% 的 CPU。已修復並有 contract test 與成長守衛擋住回歸。

**這代表在此之前所有的分析都是在壞資料上做的**，包括我自己早期的那幾次。

### 1.2 方向性交易邏輯沒有 edge

用 `trade_signal.py` 實際的進場規則（pressure dominance + delta ratio）
在 24.6 小時乾淨資料上做 symbol-demeaned、block-corrected 檢定：

| horizon | 條件 | n | blocks | 毛 alpha | 扣成本 | t |
|---|---|---|---|---|---|---|
| 1m | bias only | 1933 | 50 | +0.12 | −11.28 | 0.24 |
| 1m | bias & score≥50 | 299 | 30 | −0.66 | −12.06 | **−3.22** |
| 5m | bias & score≥30 | 1149 | 46 | +1.34 | −10.06 | −1.38 |
| 15m | bias only | 1899 | 49 | +1.34 | −10.06 | −1.25 |
| 60m | bias & score≥30 | 1039 | 45 | −1.54 | −12.94 | **−2.59** |

- 毛 alpha 最高 **+1.34 bps**，來回成本 **11.4 bps** — 差 8 倍，不是調參能補的距離
- 加上 `activity_score` 篩選讓方向性**單調變差**（1m 的 t 從 +0.24 掉到 −3.22）
- 所有 |t| > 2 的格子**全是負的**
- 多數列 40–50 blocks，高於 `MIN_RELIABLE_BLOCKS = 30`，統計是有力的

### 1.3 唯一真實的訊號是波動率

within-symbol rank IC（`activity_score` vs |forward return|）：

| horizon | mean IC | 正相關 symbol 數 |
|---|---|---|
| 1m | +0.0311 | 29/46 |
| 5m | +0.0328 | 30/46 |
| 15m | +0.0281 | 28/46 |

三個 horizon、三分之二 symbol 一致，比髒資料時期量到的 +0.0174 高一倍。

### 1.4 進場門檻從未觸發

24.6 小時內 `activity_score` 最高 **79.5**，門檻是 80，`paper_trades` 為 0 筆。
且只有 7 個 symbol 曾達到 50、4 個達到 65，其中 BTC 與 ETH 佔壓倒性多數。

---

## 2. 從證據能推出什麼、不能推出什麼

**能推出的**：`trade_signal.py` 現在這條手工規則沒有 edge。這條規則已經被
充分檢定並且失敗了。

**不能推出的**：訂單流資料裡沒有方向性訊號。我檢定的是**那一條規則**，不是
**那批資料**。OFI、多層深度失衡、CVD 背離、跨交易所領先落後——這些原始成分
從來沒有在乾淨資料上被逐一檢定過。把「一條手調的複合規則失敗」讀成「微結構
沒有 alpha」是過度推論。

這是整份計畫的樞紐：下一步不是放棄方向性，也不是繼續調那條規則，而是**回到
成分層級去測**。

**架構錯配**：系統是照方向性進出場（LONG/SHORT + TP/SL）建的，但它唯一有
預測力的東西是波動率。波動率訊號有價值，但用法是部位大小、風險濾網、或選擇權
——不是拿來猜方向。

---

## 3. 計畫

### Phase A — 止血與收尾（1–2 天）

**A1. 停用 paper trading，改為 log-only**
門檻 80 永遠不會觸發，目前它只是每秒消耗 CPU 的死路徑。不要調低門檻讓它開單
（見 §4）。把 `paper_trading.enabled` 設為 false，直到有通過 §3 gate 的假設
需要它。

**A2. 砍掉沒人讀的 dashboard payload**
`update_symbol` 每秒 46 次對最多 8 本帳 × 400 檔價位做
`model_validate().model_dump()`，而前端只用 `bid_depth_2`/`ask_depth_2`，
從未碰過原始價位。profile 顯示這條路徑約佔 25% CPU。截短到 20 檔即可，
`/api/symbol/{symbol}` 仍可用。這會為後續研究負載騰出餘裕。

**A3. push 現有分支**
`main` 目前領先 `origin/main` 30 個 commit。

### Phase B — 研究基礎設施（1 週）

`spec-signal-research-rework.md` 的 Phase 1 只完成了 `forward.py`。補齊：

- **`panel.py`** — 從 DB 建 (T, S) 對齊面板，含 tradability mask
- **`evaluate.py`** — 把 forward.py 的原語包成標準檢定流程
- **`cli.py`** — 讓一個假設能用一行指令檢定
- **`test_research_fidelity.py`** — spec 裡列為強制、仍未寫。面板重建的結果
  必須與 DB 原始查詢逐筆一致，否則後面所有數字都不可信

沒有這層，每次檢定都要重寫一次 ad-hoc 腳本（我這幾天就寫了四個），而每個
ad-hoc 腳本都是一次犯錯機會——我自己就在其中一個把 bps 乘了兩次。

### Phase C — 成分層級檢定（2–4 週，需要資料累積）

**C1. 累積跨 regime 的資料。** 目前只有 24.6 小時、單一 regime。任何結論在
一天的資料上都不算數。目標至少 3–4 週，涵蓋不同波動環境。

**C2. 逐一檢定原始成分**，每個都獨立、不預先組合：

- Order-flow imbalance（多個時間尺度）
- 訂單簿深度失衡（0.5% / 1% / 2% / 5% 各自測）
- CVD 與價格背離
- 跨交易所領先落後（Binance vs OKX，spot vs perp）
- 大單/小單成交分布
- 資金費率與 OI 變化（目前在複合分數裡權重幾乎無效）

每一項都必須：symbol-demeaned、block-corrected、報告 `n_blocks`、
留出 out-of-sample 期間。

**C3. 通過門檻（沿用 spec §4，依成本調整）**

| 條件 | taker 執行 | maker 執行 |
|---|---|---|
| 毛 alpha | > 15 bps | > 6 bps |
| \|t\| | > 2.5 | > 2.5 |
| n_blocks | > 50 | > 50 |
| out-of-sample | 同號 | 同號 |

沒有通過的成分就丟掉，不要留著「之後再組合看看」。

### Phase D — 成本側（與 C 平行）

11.4 bps 的來回成本是目前最大的單一障礙——比任何觀測到的 alpha 都大一個
數量級。這一項的改善直接放寬所有後續門檻：

- 目前假設 taker（`fee_bps: 5` × 2 + 滑價）
- Maker 掛單約可降到 4–5 bps 來回，但要付出**成交風險**與**逆選擇**成本
- 需要實測而非假設：用 replay harness 模擬 maker 掛單的成交率與逆選擇

把成本從 11.4 降到 5 bps，等於把可用訊號的門檻砍半。這可能比再找一個新特徵
更有價值。

### Phase E — 波動率分支（獨立於 C，可平行）

這是目前唯一的正結果，值得單獨發展。但先做一個 sanity check：

**E1. 它有贏過 trivial baseline 嗎？** IC +0.03 對價格聽起來不錯，但如果
「上一期已實現波動率」或一個簡單 GARCH 的 IC 更高，那 `activity_score` 就沒有
增量價值。**這一步沒過就不要往下做。**

**E2. 若有增量價值，用途排序：**
1. 部位大小 / 風險濾網（不需要新的交易邏輯，最快落地）
2. 執行時機（高波動時避免 taker 進場）
3. 選擇權（需要新的資料源與帳戶，最遠）

---

## 4. 明確的禁止事項

沿用 `spec-signal-research-rework.md` §1，並依這次的發現加強：

- **不得為了讓 paper trading 開得出單而調低 `entry_activity_score`。**
  門檻應該由「哪個區間真的有預測力」決定，而現在的證據是分數越高方向越差。
- **不得繼續在複合分數上加特徵。** 複合分數已經被證明對方向有負貢獻；
  在上面疊東西只會讓歸因更困難。成分要分開測。
- **不得刪除或放寬既有測試**，包含這次新增的 contract tests 與成長守衛。
- **不得在通過 §3 gate 之前寫任何執行/下單邏輯。**
- **不得用單一 regime 的資料下結論**，包含本文件 §1 的結論本身。

---

## 5. 建議的優先順序

如果只能做一件事：**Phase D（成本）**。11.4 bps 是比訊號品質更硬的約束，
而且它是唯一一個「改善之後所有後續工作都變容易」的項目。

如果能做兩件：加上 **Phase B（研究基礎設施）**。沒有它，Phase C 的每次檢定
都是一次手寫腳本的犯錯機會。

Phase C 需要資料累積，本來就要等，所以應該現在啟動累積、同時做 B 和 D。

Phase A 是雜務，但 A2 會直接影響 Phase C 能不能在同一台機器上跑研究負載。

---

## 6. 一個要先回答的問題

上面整份計畫假設「繼續做這個系統」。但值得先明確一件事：

24.6 小時的乾淨資料顯示，這套微結構訊號在扣掉成本後的方向性期望值是
**−10 到 −15 bps/trade**，而毛 alpha 上限是 +1.34 bps。要讓它變成正期望值，
需要 alpha 提升 8 倍或成本降到近乎零。

這不是說不可能——成分層級可能藏著比手工規則好得多的東西，而且成本側確實有
空間。但它是一個「需要數週研究、且可能得到否定答案」的專案，不是一個
「再調幾個參數就能上線」的專案。

先確認你要投入的是前者，再開始 Phase B。
