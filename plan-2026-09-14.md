# 改進計畫 — 2026-09-14

承接 `code-review-2026-09-11.md` 與 `spec-signal-research-rework.md`。這份計畫的
根據是 2026-09-13 修復基礎設施之後，第一批可信資料上跑出來的檢定結果。

---

## 1. 目前確定知道的事

### 1.1 基礎設施已經健康（2026-09-13 修復）

| | 修復前 | 現在 |
|---|---|---|
| BTC 寫入間隔 p50 | 47.15 秒 | 1.7 秒 |
| 記憶體 | 1.71 GiB 持續爬升至 SIGSEGV | ~1.25 GiB 穩態 |
| 25 小時內重啟次數 | — | 0 |
| 24.6 小時資料量 | — | 2,427,880 筆 × 46 symbols |

根因是 `LocalOrderBook.to_model()` 每次 publish 都完整排序整本 `Decimal` 帳，
而 `@depth@100ms` 是差分串流、遠端價位幾乎不會被歸零，所以帳本會隨行程壽命
無限成長。這一項佔了 73% 的 CPU。已修復並有 contract test 與成長守衛擋住回歸。

**這代表在此之前所有的分析都是在壞資料上做的**，包括我自己早期的那幾次。

### 1.2 方向性交易邏輯沒有 edge

用 `trade_signal.py` 實際的進場規則（pressure dominance + delta ratio）
在 24.6 小時乾淨資料上做 symbol-demeaned、block-corrected 檢定：

| horizon | 條件 | n | blocks | 毛 alpha | 扣成本 | t |
|---|---|---|---|---|---|---|
| 1m | bias only | 1933 | 50 | +0.12 | −11.28 | 0.24 |
| 1m | bias & score≥50 | 299 | 30 | −0.66 | −12.06 | **−3.22** |
| 5m | bias & score≥30 | 1149 | 46 | +1.34 | −10.06 | −1.38 |
| 15m | bias only | 1899 | 49 | +1.34 | −10.06 | −1.25 |
| 60m | bias & score≥30 | 1039 | 45 | −1.54 | −12.94 | **−2.59** |

- 毛 alpha 最高 **+1.34 bps**，來回成本 **11.4 bps** — 差 8 倍，不是調參能補的距離
  （上表「扣成本」欄是用 11.4 算的。Phase D 實測後 taker 是 10.0–11.2、
  blended 是 3.9–6.5 bps，所以這欄偏悲觀 1–7 bps；即使用最低的 3.9，
  每一列仍然是負的。）

> **2026-09-14 修正（Phase B 的工具跑出來才發現）**：上表的「毛 alpha」是
> **所有觀測的平均**，而 t 值是**區塊平均**算出來的。區塊大小不相等時這兩者
> 會分歧，甚至可以正負相反。用 Phase B 的工具在 24 小時乾淨資料上重跑
> response curve，改用 t 檢定真正對應的區塊平均之後：
>
> - **「分數越高方向越差」這個單調趨勢消失了。** 900s 的 pooled 平均是
>   +0.6 → −1.9 看似單調下滑，區塊平均卻是 −1.3 → +0.2，沒有方向性。
>   那個單調趨勢有相當部分是不等權重的產物。
> - **主結論不變，而且更乾淨**：60s–900s 每一格都落在 ±1.3 bps 內，
>   所有 |t| < 0.9。不是「有負 edge」，是**沒有 edge**。
>
> `BlockStat.describe()` 現在報區塊平均，並在兩者符號不同時附上 pooled 值。
> §1.2 的表是舊估計量算的，保留作為紀錄，但**「單調變差」那一條不要再引用**。
- 加上 `activity_score` 篩選讓方向性**單調變差**（1m 的 t 從 +0.24 掉到 −3.22）
- 所有 |t| > 2 的格子**全是負的**
- 多數列 40–50 blocks，高於 `MIN_RELIABLE_BLOCKS = 30`，統計是有力的

### 1.3 唯一真實的訊號是波動率

within-symbol rank IC（`activity_score` vs |forward return|）：

| horizon | mean IC | 正相關 symbol 數 |
|---|---|---|
| 1m | +0.0311 | 29/46 |
| 5m | +0.0328 | 30/46 |
| 15m | +0.0281 | 28/46 |

三個 horizon、三分之二 symbol 一致，比髒資料時期量到的 +0.0174 高一倍。

### 1.4 進場門檻從未觸發

24.6 小時內 `activity_score` 最高 **79.5**，門檻是 80，`paper_trades` 為 0 筆。
且只有 7 個 symbol 曾達到 50、4 個達到 65，其中 BTC 與 ETH 佔壓倒性多數。

---

## 2. 從證據能推出什麼、不能推出什麼

**能推出的**：`trade_signal.py` 現在這條手工規則沒有 edge。這條規則已經被
充分檢定並且失敗了。

**不能推出的**：訂單流資料裡沒有方向性訊號。我檢定的是**那一條規則**，不是
**那批資料**。OFI、多層深度失衡、CVD 背離、跨交易所領先落後——這些原始成分
從來沒有在乾淨資料上被逐一檢定過。把「一條手調的複合規則失敗」讀成「微結構
沒有 alpha」是過度推論。

這是整份計畫的樞紐：下一步不是放棄方向性，也不是繼續調那條規則，而是**回到
成分層級去測**。

**架構錯配**：系統是照方向性進出場（LONG/SHORT + TP/SL）建的，但它唯一有
預測力的東西是波動率。波動率訊號有價值，但用法是部位大小、風險濾網、或選擇權
——不是拿來猜方向。

---

## 3. 計畫

### Phase A — 止血與收尾（1–2 天）

**A1. 停用 paper trading，改為 log-only**
門檻 80 永遠不會觸發，目前它只是每秒消耗 CPU 的死路徑。不要調低門檻讓它開單
（見 §4）。把 `paper_trading.enabled` 設為 false，直到有通過 §3 gate 的假設
需要它。

**A2. 砍掉沒人讀的 dashboard payload**
`update_symbol` 每秒 46 次對最多 8 本帳 × 400 檔價位做
`model_validate().model_dump()`，而前端只用 `bid_depth_2`/`ask_depth_2`，
從未碰過原始價位。profile 顯示這條路徑約佔 25% CPU。截短到 20 檔即可，
`/api/symbol/{symbol}` 仍可用。這會為後續研究負載騰出餘裕。

**A3. push 現有分支**
`main` 目前領先 `origin/main` 30 個 commit。

### Phase B — 研究基礎設施 ✅ 已完成 2026-09-14

四項全部落地：

- **`panel.py`** — 從 DB 重建 (T, S) 對齊面板，含 tradability mask。
  **呼叫 `app.scoring` / `app.trade_signal` 的真實函式**，不複製一份邏輯。
- **`evaluate.py`** — 分位數 response curve、rank IC（pooled 與 within-symbol）、
  三重障礙回測（**每次都並列一組隨機進場對照組**，共用同一組 block 分割）
- **`cli.py`** — `python -m research {feed-health,fidelity,evaluate,backtest}`
- **`test_research_fidelity.py`** — spec 列為強制的那一項，先寫測試再寫實作

#### 保真度：兩層檢查，缺一不可

| 層 | 問的問題 | 結果 |
|---|---|---|
| 單元測試（14 項） | 時間戳對齊的合成資料上，聚合邏輯是否忠實？ | 逐格相等 |
| `research fidelity` | 真實資料上，離線重建是否還等於線上？ | 8,280 格，p50 **0.0000** |

**第二層不是多餘的。** 真實資料上有一個單元測試看不到的問題：
`flow_metrics.timestamp` 是**寫入時刻**，`market_metrics.timestamp` 是
**交易所產生 book 的時刻**。兩者在同一個網格上對齊時，面板可能把某個 flow 窗
配上跟線上當時不同一個 tick 的深度。深度是 pressure 的分母，而 pressure 佔
100 分裡的 50 分，所以**快速變動的幣種**會有幾個點的落差。

實測 8,280 格：p50 0.00、p90 0.36、p99 2.97、max 14.69，96% 在 1 分以內。
超過 1 分的落差中有 **94% 的 `cross_exchange_state` 與 `oi_change_5m` 完全相同**，
確認落差來自 pressure 項而不是別處。BTC/ETH 最嚴重（p99 約 8.9 / 6.5）。

因此 `fidelity` 指令**以中位數為門檻、不以尾部為門檻**：尾部是這兩個時鐘的
必然結果，中位數才是「聚合邏輯有沒有走樣」的偵測器（正常值 0.00，門檻 0.5）。

**對 Phase C 的意義**：逐 venue 的原始欄位（obi、各檔深度、成交量、CVD…）
是直接從資料列讀出來的，**完全精確**，不受這個問題影響。只有衍生的複合值
（`activity_score`、`bias`）帶著這個配對噪音。而 Phase C 要測的正是原始成分。

#### 記憶體：一個實測出來的坑

第一版把整個視窗的資料列全部讀進記憶體。實測 338,400 列吃掉 566 MB，
外推到真實的一天是 **~7.9 GB** —— 不是慢，是跑不動。而且在幾百列的合成測試上
完全看不出來。

加了一條守衛（網格固定、底下的資料列密度 ×8，記憶體不得超過 ×3），改成
串流：資料列與網格都按時間排序，一次前向掃描只保留每個 (symbol, venue) 的
最新一列。**8.6× → 1.22×**，真實 24 小時面板的常駐記憶體 **103 MB**。
重寫不改任何數值——對線上的保真度數字一格不差。

#### 執行成本（實測，線性）

| 視窗 | 網格 | 建面板 | rank_ic | response_curve |
|---|---|---|---|---|
| 1h | 361 | 12.4s | 0.04s | 0.01s |
| 2h | 721 | 24.0s | 0.07s | 0.01s |
| 4h | 1,441 | 47.9s | 0.13s | 0.01s |
| 8h | 2,881 | 98.3s | 0.22s | 0.03s |

每加倍剛好翻一倍，所以 24 小時約 **5 分鐘**。統計本身的成本可以忽略，
**全部時間都在建面板**，而且是 I/O 而非計算：24 小時視窗底下有 **1,880 萬列**
（market 791 萬、flow 829 萬、signal 234 萬、derivative 30 萬）。

下一個可拿的優化（尚未做）：面板在 10 秒網格上每個 (symbol, venue) 最多只用得到
一列，但資料是 1.7 秒一列，**約 6 倍是白讀的**。把降採樣下推到 SQL
（每個網格桶取最後一列）應該能拿到接近 6 倍。記憶體已經不是問題，這只是時間，
留到 Phase C 真的嫌慢時再做。

沒有這一層，每次檢定都要重寫一次 ad-hoc 腳本——這幾天寫了六個，其中兩個是錯的：
一個把 bps 乘了兩次（報出 1883% 的 alpha），一個把每個 symbol 的逆選擇塌成同一個
常數（成本差了 0.4 bps）。兩個都是靠「數字看起來不合理」才被抓到，那不是控制手段。

### Phase C — 成分層級檢定（2–4 週，需要資料累積）

**C1. 累積跨 regime 的資料。** 目前只有 24.6 小時、單一 regime。任何結論在
一天的資料上都不算數。目標至少 3–4 週，涵蓋不同波動環境。

**C2. 逐一檢定原始成分**，每個都獨立、不預先組合：

- Order-flow imbalance（多個時間尺度）
- 訂單簿深度失衡（0.5% / 1% / 2% / 5% 各自測）
- CVD 與價格背離
- 跨交易所領先落後（Binance vs OKX，spot vs perp）
- 大單/小單成交分布
- 資金費率與 OI 變化（目前在複合分數裡權重幾乎無效）

每一項都必須：symbol-demeaned、block-corrected、報告 `n_blocks`、
留出 out-of-sample 期間。

**C3. 通過門檻（依 Phase D 實測成本重訂）**

原本的 15 / 6 bps 是假設值。成本已經在 2026-09-14 實測完並寫進
`backend/research/costs.py`，門檻改用實測值 × 1.3 的安全邊際：

| 條件 | taker 執行 | blended（實際可用） | maker 全成交（樂觀上界） |
|---|---|---|---|
| 實測來回成本 | 10.0–11.2 bps | 3.9–6.5 bps | 2.1–3.3 bps |
| 毛 alpha 門檻 | > 15 bps | > 8.5 bps | > 4.5 bps |
| \|t\| | > 2.5 | > 2.5 | > 2.5 |
| n_blocks | > 50 | > 50 | > 50 |
| out-of-sample | 同號 | 同號 | 同號 |

**實際要用的是 blended 那一欄。** maker 全成交那欄是上界，不是門檻：成交率
是「排在隊伍最前面」的假設下量到的，真實掛單會排在隊伍後面，而聚合指標裡
沒有任何隊列位置資訊。在 fill simulator 做出來之前，用那一欄等於自欺。

沒有通過的成分就丟掉，不要留著「之後再組合看看」。

### Phase D — 成本側 ✅ 已完成 2026-09-14

原本這一節寫的是「需要實測而非假設」。實測做完了，模型固定在
`backend/research/costs.py`，由先寫的 oracle `tests/test_costs_contract.py`
釘住算術。

**輸入全部經過查證或實測，沒有沿用假設值：**

| 輸入 | 來源 | 值 |
|---|---|---|
| 手續費 | Binance USDⓈ-M / OKX perp 官方費率表，VIP0/Lv1 | 0.02% maker / 0.05% taker |
| 價差 | 本系統資料實測（單位已對過 tick） | BTC 0.013、ETH 0.040、SOL 0.99、FIL 1.19 bps |
| 逆選擇 | 被動成交後 mid-to-mid，5–60 秒 | −0.16 至 −0.38 bps（**有利**，價格是回歸的） |
| 成交率 | 隊首假設下的上界，60 秒 | 55%–80% |

**來回成本（模型輸出，各 symbol 用自己的 60 秒逆選擇與成交率）：**

| symbol | taker | maker 全成交 | blended@60s | 相對 taker |
|---|---|---|---|---|
| BTC | 10.01 | 3.25 | **6.29** | −37% |
| ETH | 10.04 | 3.32 | **5.64** | −44% |
| SOL | 10.99 | 2.69 | **6.52** | −41% |
| FIL | 11.19 | 2.05 | **3.89** | −65% |

**結論：maker 執行把來回成本從 ~10–11 bps 降到 ~4–6.5 bps，門檻砍掉四成。
但它救不了現在這條策略**——毛 alpha 是 +1.34 bps，上表最便宜的一格是 2.05。
Phase D 降低了 Phase C 的門檻，沒有跨過它。

順帶修正：先前所有期望值計算用的 11.4 bps 假設 1.4 bp 價差，主流幣真實
taker 成本是 ~10.0 bps。也就是說先前的數字**偏悲觀約 1.4 bps**，不影響任何
結論。

**兩個不能被埋掉的但書：**

1. **成交率是上界，而且大概是鬆的。** 它假設掛單排在隊首。真實掛單排在隊伍
   後面，隊伍可以在還沒輪到你之前就被吃完，而聚合指標裡沒有任何隊列資訊。
   blended 那一欄是樂觀端。要收緊需要 Phase B 的 replay harness 做 fill
   simulator。
2. **「逆選擇是有利的」這個結果本身可疑，值得存疑。** 一檔寬的簿子，mid 的
   跳動就可能製造出假的回歸。我認為這不是跳動假象的理由是：跳動會在一個 tick
   內飽和然後打平，但實測的回歸是**隨 horizon 增長**的（BTC 5s→60s 為
   −0.22→−0.37 bps），而 −0.37 bps 在 BTC 上約等於 28 個 tick，遠大於報價
   跳動能解釋的幅度。但要真正定案需要逐筆成交資料，而我們沒有存。

### Phase E — 波動率分支（獨立於 C，可平行）

這是目前唯一的正結果，值得單獨發展。但先做一個 sanity check：

**E1. 它有贏過 trivial baseline 嗎？** IC +0.03 對價格聽起來不錯，但如果
「上一期已實現波動率」或一個簡單 GARCH 的 IC 更高，那 `activity_score` 就沒有
增量價值。**這一步沒過就不要往下做。**

**E2. 若有增量價值，用途排序：**
1. 部位大小 / 風險濾網（不需要新的交易邏輯，最快落地）
2. 執行時機（高波動時避免 taker 進場）
3. 選擇權（需要新的資料源與帳戶，最遠）

---

## 4. 明確的禁止事項

沿用 `spec-signal-research-rework.md` §1，並依這次的發現加強：

- **不得為了讓 paper trading 開得出單而調低 `entry_activity_score`。**
  門檻應該由「哪個區間真的有預測力」決定，而現在的證據是分數越高方向越差。
- **不得繼續在複合分數上加特徵。** 複合分數已經被證明對方向有負貢獻；
  在上面疊東西只會讓歸因更困難。成分要分開測。
- **不得刪除或放寬既有測試**，包含這次新增的 contract tests 與成長守衛。
- **不得在通過 §3 gate 之前寫任何執行/下單邏輯。**
- **不得用單一 regime 的資料下結論**，包含本文件 §1 的結論本身。

---

## 5. 建議的優先順序

~~如果只能做一件事：**Phase D（成本）**。~~ 已完成，見上。成本從 ~11.4 降到
blended ~4–6.5 bps，所有後續門檻同步下修。

~~現在只能做一件事的話：**Phase B（研究基礎設施）**。~~ 也完成了，見上。

**現在該做的是 Phase C 的 C1：讓資料累積。** 工具已經備齊，剩下的是時間——
目前只有單一 regime 的資料，目標至少 3–4 週。累積期間可以做 Phase A（雜務）
與 Phase E1（波動率的 trivial baseline 對照），兩者都不需要更多資料。

Phase A 是雜務，但 A2 會直接影響 Phase C 能不能在同一台機器上跑研究負載——
現在多了一個理由：建一次 24 小時面板要 5 分鐘的 CPU，跟線上收集器搶同一台機器。

---

## 6. 一個要先回答的問題

上面整份計畫假設「繼續做這個系統」。但值得先明確一件事：

24.6 小時的乾淨資料顯示，這套微結構訊號的毛 alpha 上限是 +1.34 bps，而扣掉
成本後的方向性期望值，用 taker 是 −10 到 −15 bps/trade，用 Phase D 實測出來
最好的 blended 執行仍然是 **−2.6 bps/trade**。成本側已經做完了，把門檻砍掉
四成之後距離還是負的。剩下的路只有 alpha 那一邊。

這不是說不可能——成分層級可能藏著比手工規則好得多的東西。但成本這條比較容易
的路已經走完了，而它不夠。這是一個「需要數週研究、且可能得到否定答案」的
專案，不是一個「再調幾個參數就能上線」的專案。

先確認你要投入的是前者，再開始 Phase B。
